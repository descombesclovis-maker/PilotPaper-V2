name: Build PilotPaper V2 Windows

on:
  workflow_dispatch:
  push:
    branches:
      - release/v2
    paths:
      - "desktop/**"
      - "app/**"
      - "components/**"
      - "geometry-engine/**"
      - "lib/**"
      - "public/**"
      - "scripts/**"
      - "tests/**"
      - "worker/**"
      - "package.json"
      - "package-lock.json"
      - "vite.config.ts"
      - "tw-animate-css"
      - ".github/workflows/build-v2-windows.yml"

permissions:
  contents: write

concurrency:
  group: pilotpaper-v2-windows
  cancel-in-progress: true

jobs:
  windows-v2:
    runs-on: windows-latest
    timeout-minutes: 90
    steps:
      - name: Checkout V2
        uses: actions/checkout@v4

      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22.23.2
          cache: npm

      - name: Install locked dependencies
        shell: pwsh
        run: npm ci

      - name: Build web application
        shell: pwsh
        run: npm run build

      - name: Run PilotPaper regressions
        shell: pwsh
        run: npm test

      - name: Use Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: geometry-engine/requirements.txt

      - name: Install geometry engine dependencies
        shell: pwsh
        run: |
          python -m pip install --disable-pip-version-check -r geometry-engine/requirements.txt pyinstaller
          if ($LASTEXITCODE -ne 0) { throw "Geometry dependencies installation failed" }

      - name: Validate geometry engine modules
        shell: pwsh
        run: |
          python -m py_compile geometry-engine/app.py geometry-engine/main.py geometry-engine/registration.py geometry-engine/photo_api.py geometry-engine/photogrammetry_api.py geometry-engine/run.py
          if ($LASTEXITCODE -ne 0) { throw "Geometry Python syntax validation failed" }

      - name: Build embedded geometry engine
        shell: pwsh
        run: |
          python -m PyInstaller --noconfirm --clean --onefile `
            --name PilotPaperGeometryEngine `
            --paths geometry-engine `
            --collect-all rasterio `
            --collect-all shapely `
            --collect-all pyproj `
            --collect-all sklearn `
            --collect-all cv2 `
            --collect-all laspy `
            --collect-all PIL `
            --hidden-import uvicorn.logging `
            --hidden-import uvicorn.loops.auto `
            --hidden-import uvicorn.protocols.http.auto `
            --hidden-import uvicorn.protocols.websockets.auto `
            --hidden-import uvicorn.lifespan.on `
            geometry-engine/run.py
          if ($LASTEXITCODE -ne 0 -or -not (Test-Path "dist/PilotPaperGeometryEngine.exe")) {
            throw "PilotPaperGeometryEngine.exe was not created"
          }

      - name: Smoke-test embedded geometry engine
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $out = Join-Path $PWD "geometry-smoke.out.log"
          $err = Join-Path $PWD "geometry-smoke.err.log"
          $engine = Start-Process -FilePath "dist/PilotPaperGeometryEngine.exe" -PassThru -WindowStyle Hidden -RedirectStandardOutput $out -RedirectStandardError $err
          try {
            $healthy = $false
            for ($i = 0; $i -lt 120; $i++) {
              Start-Sleep -Milliseconds 500
              try {
                $health = Invoke-RestMethod -Uri "http://127.0.0.1:8765/health" -TimeoutSec 2
                if ($health.ok -eq $true) {
                  if (-not ($health.capabilities -contains "geotiff-dsm")) { throw "DSM capability missing" }
                  if (-not ($health.capabilities -contains "sampled-elevation-points")) { throw "IGN sampled elevation capability missing" }
                  if (-not ($health.capabilities -contains "metric-obstacles")) { throw "Metric obstacle capability missing" }
                  if (-not ($health.capabilities -contains "roof-edge-topology")) { throw "Roof edge topology capability missing" }
                  if (-not ($health.capabilities -contains "opencv-registration")) { throw "Photo registration capability missing" }
                  $healthy = $true
                  break
                }
              } catch {}
            }
            if (-not $healthy) {
              if (Test-Path $out) { Get-Content $out -Tail 100 }
              if (Test-Path $err) { Get-Content $err -Tail 100 }
              throw "Embedded geometry engine health check failed"
            }
          } finally {
            if (-not $engine.HasExited) { Stop-Process -Id $engine.Id -Force -ErrorAction SilentlyContinue }
          }

      - name: Build Windows icon
        shell: pwsh
        run: |
          python -m pip install --disable-pip-version-check pillow
          @'
          from PIL import Image
          from pathlib import Path
          source = Path("public/pilotpaper-monogram.png")
          target = Path("desktop/pilotpaper.ico")
          image = Image.open(source).convert("RGBA")
          image.thumbnail((256, 256), Image.Resampling.LANCZOS)
          canvas = Image.new("RGBA", (256, 256), (0, 0, 0, 0))
          canvas.alpha_composite(image, ((256-image.width)//2, (256-image.height)//2))
          canvas.save(target, format="ICO", sizes=[(16,16),(24,24),(32,32),(48,48),(64,64),(128,128),(256,256)])
          '@ | Set-Content -Path make_icon.py -Encoding utf8
          python make_icon.py
          if (-not (Test-Path desktop/pilotpaper.ico)) { throw "PilotPaper icon was not created" }

      - name: Use .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Compile V2 WebView2 launcher
        shell: pwsh
        run: >-
          dotnet publish desktop/PilotPaperLauncher/PilotPaperLauncher.csproj
          -c Release
          -r win-x64
          --self-contained true
          -p:PublishSingleFile=true
          -p:DebugType=None
          -p:DebugSymbols=false

      - name: Assemble V2 payload
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $stage = Join-Path $PWD "desktop-stage-v2"
          $current = Join-Path $stage "app/current"
          New-Item -ItemType Directory -Force -Path $current | Out-Null
          New-Item -ItemType Directory -Force -Path (Join-Path $current "runtime") | Out-Null

          $launcher = "desktop/PilotPaperLauncher/bin/Release/net8.0-windows/win-x64/publish/PilotPaper-V2.exe"
          if (-not (Test-Path $launcher)) { throw "PilotPaper-V2.exe missing" }
          Copy-Item $launcher (Join-Path $stage "PilotPaper-V2.exe") -Force
          Copy-Item "desktop/pilotpaper.ico" (Join-Path $stage "pilotpaper.ico") -Force
          Copy-Item (Get-Command node.exe).Source (Join-Path $current "runtime/node.exe") -Force
          Copy-Item "dist/PilotPaperGeometryEngine.exe" (Join-Path $current "runtime/PilotPaperGeometryEngine.exe") -Force
          Set-Content -Path (Join-Path $stage "PILOTPAPER-BUILD.txt") -Encoding ascii -Value "${{ github.sha }}"

          $tracked = git ls-files
          foreach ($file in $tracked) {
            if ($file -like ".github/*" -or $file -like "desktop/*" -or $file -like "tests/*" -or $file -like "geometry-engine/*") { continue }
            if ($file -like "DEMARRER-PILOTPAPER.cmd" -or $file -like "INSTALLER-PILOTPAPER.cmd" -or $file -like "INSTALLATION-WINDOWS.txt") { continue }
            $target = Join-Path $current $file
            $parent = Split-Path -Parent $target
            if ($parent) { New-Item -ItemType Directory -Force -Path $parent | Out-Null }
            Copy-Item $file $target -Force
          }

          robocopy node_modules (Join-Path $current "node_modules") /E /NFL /NDL /NJH /NJS /NP
          $code = $LASTEXITCODE
          if ($code -ge 8) { throw "robocopy node_modules failed with code $code" }
          $global:LASTEXITCODE = 0

          Set-Content -Path (Join-Path $stage "V2-README.txt") -Encoding utf8 -Value @"
          PilotPaper V2 — Preview technique
          =================================
          - V1 reste séparée et intacte.
          - Moteur géométrique métrique embarqué et démarré automatiquement.
          - Moteur géométrique distant optionnel avec fallback PilotPaper.
          - Jeton technique attendu dans le stockage protégé Windows, jamais dans le payload.
          - Cette build est destinée aux validations V2 avant publication finale.
          "@

          foreach ($required in @(
            (Join-Path $current "runtime/node.exe"),
            (Join-Path $current "runtime/PilotPaperGeometryEngine.exe"),
            (Join-Path $current "node_modules/vite/bin/vite.js"),
            (Join-Path $current "vite.config.ts"),
            (Join-Path $stage "PilotPaper-V2.exe"),
            (Join-Path $stage "PILOTPAPER-BUILD.txt")
          )) { if (-not (Test-Path $required)) { throw "V2 payload missing: $required" } }

      - name: Install Inno Setup
        shell: pwsh
        run: choco install innosetup --no-progress -y

      - name: Build V2 installer
        shell: pwsh
        run: |
          $iscc = "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe"
          & $iscc "desktop\PilotPaperV2Installer.iss"
          if ($LASTEXITCODE -ne 0) { throw "Inno Setup failed" }
          if (-not (Test-Path "desktop-output-v2/PilotPaper-V2-Setup.exe")) { throw "V2 installer missing" }

          $hash = (Get-FileHash "desktop-output-v2/PilotPaper-V2-Setup.exe" -Algorithm SHA256).Hash.ToLowerInvariant()
          "$hash  PilotPaper-V2-Setup.exe" | Set-Content -Path "desktop-output-v2/PilotPaper-V2-Setup.exe.sha256" -Encoding ascii
          if (-not (Test-Path "desktop-output-v2/PilotPaper-V2-Setup.exe.sha256")) { throw "V2 checksum missing" }

      - name: Upload V2 installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: PilotPaper-V2-Setup
          path: |
            desktop-output-v2/PilotPaper-V2-Setup.exe
            desktop-output-v2/PilotPaper-V2-Setup.exe.sha256
          if-no-files-found: error
          retention-days: 14

      - name: Publish validated V2 update channel
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $ErrorActionPreference = "Stop"
          $tag = "pilotpaper-v2-latest"
          $title = "PilotPaper V2 — Mise à jour validée"
          $notes = "Canal de mise à jour PilotPaper V2. Cette release n'est publiée qu'après build, tests, smoke test du moteur métrique et compilation Windows réussis.`n`nPilotPaper-Build-SHA: $env:GITHUB_SHA"
          $setup = "desktop-output-v2/PilotPaper-V2-Setup.exe"
          $hash = "desktop-output-v2/PilotPaper-V2-Setup.exe.sha256"

          gh release view $tag --repo $env:GITHUB_REPOSITORY 2>$null | Out-Null
          $exists = $LASTEXITCODE -eq 0
          $global:LASTEXITCODE = 0

          if ($exists) {
            gh release upload $tag $setup $hash --repo $env:GITHUB_REPOSITORY --clobber
            if ($LASTEXITCODE -ne 0) { throw "Unable to replace V2 update assets" }
            gh release edit $tag --repo $env:GITHUB_REPOSITORY --title $title --notes $notes
            if ($LASTEXITCODE -ne 0) { throw "Unable to update V2 release metadata" }
          } else {
            gh release create $tag $setup $hash --repo $env:GITHUB_REPOSITORY --target $env:GITHUB_SHA --title $title --notes $notes --prerelease
            if ($LASTEXITCODE -ne 0) { throw "Unable to create V2 update release" }
          }
