# PilotPaper local secrets - copy to .env.local, never commit real values.

# Existing visual engine
OPENAI_API_KEY=

# PilotPaper V2 / OpenSolar POC
# Keep false until the POC is visually validated against real French roofs.
OPENSOLAR_ENABLED=false
OPENSOLAR_ORG_ID=
OPENSOLAR_BEARER_TOKEN=
